# Press

Kind: **person** (journalist / host). Sources: `web`, `youtube`. Score: presence.

Angles:

1. `{topic} (journalist OR reporter OR "staff writer" OR byline)`
2. `{topic} (interview OR "talks to" OR "spoke with")`
3. YouTube `{topic} interview`
4. `site:substack.com {topic}`

Web fallback identity is `person/web/{name-slug}` from the title. Noisy. Prefer a byline URL the parser can hang a name on.

**Use when:** journalists, reporters, press, coverage, media, podcast hosts.

**Do not:** dump outlet homepages as people; treat a brand YouTube as a reporter; mix this ranking with creator engagement.
